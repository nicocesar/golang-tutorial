{
  "hostRequirements": {
    "cpus": 2,
    "memory": "4gb", // otherwise is bytes - allows for other units like mb, or k
    "storage": "32gb" // otherwise is bytes - allows for other units like tb, or mb, or k
  },
  "build": { "dockerfile": "Dockerfile" },
   //"extensions": ["dbaeumer.vscode-eslint"],
  "forwardPorts": [3000]
}